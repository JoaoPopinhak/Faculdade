#include <stdio.h>
int main (){
    float peso;

    printf("Insira seu peso");
	scanf("%f", & peso);
	
    printf("O peso se a pessoa engordar 15 kg e: %f" , peso = peso + (peso * 0.15));
    printf("Se emagrecer 20 porcento e : %f", peso = peso - (peso * 0.20));
}